---
import { Image } from "astro:assets";
import { Schema } from "astro-seo-schema";
import { getCollection, render } from "astro:content";
import { of } from "true-myth/maybe";
import BlogArticleByline from "../../blog/BlogArticleByline.astro";
import BlogBackLink from "../../blog/BlogBackLink.astro";
import BlogPostContent from "../../blog/BlogPostContent.astro";
import BlogPostMentions from "../../blog/BlogPostMentions.astro";
import BlogShell from "../../blog/BlogShell.astro";
import BlogTopicLink from "../../blog/BlogTopicLink.astro";
import LocalizedDateTime from "../../blog/LocalizedDateTime.astro";
import LocalizedDateTimeScript from "../../blog/LocalizedDateTimeScript.astro";
import { getBlogPostTopicDetails } from "../../blog/blog-post-topics.js";
import {
	createBlogPostPageTitle,
	createBlogPostReadingTimeLabel,
	sortBlogPostsByPublicationDateDescending
} from "../../blog/blog-posts.js";
import {
	createAbsoluteAssetUrl,
	createBlogPostAbsoluteUrl,
	createBlogRssFeedAbsoluteUrl,
	getConfiguredSiteUrlOrThrow
} from "../../blog/blog-site.js";
import aboutImage from "../../assets/images/about.jpg";
import headerImage from "../../assets/images/header-01.jpg";

export async function getStaticPaths() {
	const blogPosts = sortBlogPostsByPublicationDateDescending(await getCollection("blog"));

	return blogPosts.map((blogPost) => {
		return {
			params: {
				slug: blogPost.id
			},
			props: {
				blogPost
			}
		};
	});
}

const { blogPost } = Astro.props;
const { Content } = await render(blogPost);
const configuredSiteUrl = getConfiguredSiteUrlOrThrow(Astro.site);
const blogPostUrl = createBlogPostAbsoluteUrl(configuredSiteUrl, blogPost.id);
const blogRssFeedUrl = createBlogRssFeedAbsoluteUrl(configuredSiteUrl);
const openGraphImageUrl = createAbsoluteAssetUrl(configuredSiteUrl, headerImage.src);
const openGraphImageAlternativeText = "echooff.dev header illustration";
const topicDetails = getBlogPostTopicDetails(blogPost.data.topic);
const blogPostReadingTimeLabel = createBlogPostReadingTimeLabel(blogPost.body ?? "");
const blogPostAuthorName = "Christian Rackerseder";
const siteUrl = configuredSiteUrl.toString();
const blogPostUpdatedAt = of(blogPost.data.updatedAt);
---

<BlogShell
	canonicalUrl={blogPostUrl}
	description={blogPost.data.description}
	openGraphImageAlternativeText={openGraphImageAlternativeText}
	openGraphImageUrl={openGraphImageUrl}
	openGraphModifiedTime={blogPostUpdatedAt}
	openGraphPublishedTime={blogPost.data.publishedAt}
	openGraphType="article"
	openGraphUrl={blogPostUrl}
	rssFeedUrl={blogRssFeedUrl}
	robots="index,follow,max-image-preview:large"
	title={createBlogPostPageTitle(blogPost.data.title)}
	twitterCardType="summary_large_image"
	pageTitle="Blog"
	showReadingProgressIndicator={true}
	showPageHeader={false}
>
	<Fragment slot="search-engine-optimization-schema">
		<Schema
			item={{
				"@context": "https://schema.org",
				"@type": "BlogPosting",
				author: {
					"@type": "Person",
					name: blogPostAuthorName,
					url: siteUrl
				},
				datePublished: blogPost.data.publishedAt,
				...blogPostUpdatedAt
					.map((updatedAt) => {
						return { dateModified: updatedAt };
					})
					.unwrapOr({}),
				description: blogPost.data.description,
				headline: blogPost.data.title,
				image: openGraphImageUrl,
				articleSection: topicDetails.label,
				mainEntityOfPage: blogPostUrl,
				publisher: {
					"@type": "Person",
					name: blogPostAuthorName,
					url: siteUrl
				},
				url: blogPostUrl
			}}
		/>
	</Fragment>

	<article class="blog-post-article h-entry" data-pagefind-body>
		<header class="blog-post-article-header">
			<nav aria-label="Blog" class="blog-post-article-navigation">
				<BlogBackLink href="/blog">Back to Blog</BlogBackLink>
			</nav>
			<section class="blog-post-article-heading-group" aria-label="Article title and author">
				<h1 class="p-name">{blogPost.data.title}</h1>
				<div class="blog-post-article-author-row" data-pagefind-ignore="all">
					<figure class="blog-post-article-avatar h-card">
						<a class="u-url" href={siteUrl} rel="author">
							<Image
								alt={blogPostAuthorName}
								class="blog-post-article-avatar-image u-photo"
								height={88}
								src={aboutImage}
								quality={75}
								width={88}
							/>
						</a>
						<span class="blog-post-screen-reader-only p-name">{blogPostAuthorName}</span>
					</figure>
					<div class="blog-post-article-author-copy">
						<BlogArticleByline authorName={blogPostAuthorName} authorUrl={siteUrl} />
						<p
							class:list={[
								"blog-post-article-metadata",
								{ "blog-post-article-metadata-with-update": blogPostUpdatedAt.isJust }
							]}
						>
							<span class="blog-post-article-metadata-item blog-post-article-date-metadata-item">
								Published <LocalizedDateTime
									className="dt-published"
									dateTime={blogPost.data.publishedAt}
								/>
							</span>
							{
								blogPostUpdatedAt
									.map((updatedAt) => {
										return (
											<span class="blog-post-article-metadata-item blog-post-article-date-metadata-item">
												Updated{" "}
												<LocalizedDateTime className="dt-updated" dateTime={updatedAt} />
											</span>
										);
									})
									.unwrapOr("")
							}
							<span class="blog-post-article-metadata-item">
								in <BlogTopicLink href={`/blog/topics/${topicDetails.slug}`}
									>{topicDetails.label}</BlogTopicLink
								>
							</span>
							<span class="blog-post-article-metadata-item blog-post-article-reading-time">
								{blogPostReadingTimeLabel}
							</span>
						</p>
					</div>
				</div>
			</section>
			<a class="u-url blog-post-screen-reader-only" data-pagefind-ignore="all" href={blogPostUrl}> Permalink </a>
		</header>

		<section class="blog-post-article-body" aria-label="Article content">
			<BlogPostContent>
				<Content />
			</BlogPostContent>

			<div data-pagefind-ignore="all">
				<BlogPostMentions blogPostId={blogPost.id} server:defer>
					<section class="blog-post-mentions-loading" aria-live="polite" slot="fallback">
						<p>Loading mentions...</p>
					</section>
				</BlogPostMentions>
			</div>
		</section>
	</article>

	<LocalizedDateTimeScript />
</BlogShell>

<style lang="scss">
	@use "../../styles/variables";
	@use "../../styles/mixins";

	.blog-post-article {
		display: flex;
		flex-direction: column;
	}

	.blog-post-article-header {
		@include mixins.blog-shell-page-header-surface(1.5rem);
		display: flex;
		flex-direction: column;
		gap: 1.25rem;
		padding-bottom: 1.25rem;
	}

	.blog-post-article-navigation {
		display: flex;
	}

	.blog-post-article-heading-group {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.blog-post-article-author-row {
		align-items: center;
		display: flex;
		gap: 0.9rem;
	}

	.blog-post-article-avatar {
		flex-shrink: 0;
		margin: 0;
	}

	.blog-post-article-avatar-image {
		border: 4px solid variables.$comment;
		border-radius: 9999px;
		display: block;
		height: 4.5rem;
		width: 4.5rem;
	}

	.blog-post-article-author-copy {
		display: flex;
		flex-direction: column;
		gap: 0.4rem;
		min-width: 0;
	}

	.blog-post-screen-reader-only {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0, 0, 0, 0);
		white-space: nowrap;
		border: 0;
	}

	.blog-post-mentions-loading {
		border-top: 1px solid rgba(variables.$selection, 0.8);
		color: rgba(variables.$foreground, 0.62);
		margin-top: 2rem;
		padding-top: 1.5rem;
	}

	.blog-post-mentions-loading p {
		margin: 0;
	}

	.blog-post-article-metadata {
		color: rgba(variables.$foreground, 0.46);
		font-size: 0;
		margin: 0;
	}

	.blog-post-article-metadata-item {
		display: inline;
		font-size: 0.82rem;
	}

	.blog-post-article-date-metadata-item {
		white-space: nowrap;
	}

	.blog-post-article-metadata-item:not(:last-child)::after {
		color: rgba(variables.$foreground, 0.3);
		content: " · ";
	}

	.blog-post-article-reading-time {
		letter-spacing: -0.01em;
	}

	h1 {
		color: variables.$foreground;
		font-size: clamp(1.75rem, 3vw, 2.35rem);
		font-weight: 800;
		letter-spacing: -0.04em;
		line-height: 1.1;
	}

	.blog-post-article-body {
		padding: 1rem 1.5rem 1.5rem;
	}

	@media (min-width: variables.$small-width) {
		.blog-post-article-header {
			@include mixins.blog-shell-page-header-surface(2rem);
			padding-bottom: 1.25rem;
		}

		.blog-post-article-author-row {
			gap: 1rem;
		}

		.blog-post-article-avatar-image {
			height: 4.75rem;
			width: 4.75rem;
		}

		.blog-post-article-body {
			padding-left: 2rem;
			padding-right: 2rem;
		}
	}

	@media (max-width: calc(variables.$small-width - 0.02rem)) {
		.blog-post-article-header {
			gap: 1rem;
		}

		.blog-post-article-heading-group {
			gap: 0.85rem;
		}

		.blog-post-article-author-row {
			align-items: center;
			display: grid;
			gap: 0.55rem 0.75rem;
			grid-template-columns: auto 1fr;
		}

		.blog-post-article-avatar-image {
			height: 3.5rem;
			width: 3.5rem;
		}

		.blog-post-article-author-copy {
			display: contents;
		}

		.blog-post-article-metadata {
			grid-column: 1 / -1;
		}

		.blog-post-article-metadata {
			align-items: baseline;
			column-gap: 0.5rem;
			display: flex;
			font-size: 0.82rem;
			flex-wrap: wrap;
			row-gap: 0.2rem;
		}

		.blog-post-article-metadata-item {
			display: block;
			font-size: inherit;
			white-space: nowrap;
		}

		.blog-post-article-metadata-item:not(:last-child)::after {
			content: none;
		}

		.blog-post-article-metadata-with-update .blog-post-article-date-metadata-item {
			flex-basis: 100%;
		}
	}
</style>
