---
interface Props {
	readonly ariaLabel: string;
}

const { ariaLabel } = Astro.props;
---

<cite aria-label={ariaLabel} class="mt-2 text-lg not-italic font-bold text-dracula-green">
	<slot />
</cite>
