---
interface Props {
	readonly ariaLabel: string;
}

const { ariaLabel } = Astro.props;
---

<cite aria-label={ariaLabel} class="text-lg text-dracula-green font-bold not-italic mt-2">
	<slot />
</cite>
