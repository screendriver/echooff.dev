---
when:
    - event: [push, manual]

steps:
    - name: install
      image: node:24.8.0
      commands:
          - npm clean-install

    - name: test
      image: node:24.8.0
      commands:
          - npx just test
          - npx just build
